package com.theinfectedhour.multiplayer;

/** Player 2's session: sends input packets only (never position), applies authoritative state from the host. */
public class ClientSession {
    // TODO: implement
}
