package com.theinfectedhour.backend.config;

import org.springframework.context.annotation.Configuration;

/** Central place for authentication/authorization configuration — empty until the auth design lands. */
@Configuration
public class SecurityConfig {
    // TODO: implement
}
